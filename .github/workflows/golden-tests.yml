name: Golden Tests

on:
  push:
  pull_request:

jobs:
  golden-tests:
    runs-on: ubuntu-latest

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true   # Future-proof GitHub Actions

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -e .[dev]

      - name: Validate manifests
        run: python tools/validate_manifest.py

      - name: Run golden tests
        run: pytest tests/golden --diff-golden -q
