# Installation

Install textfsmgen using pip:

```
pip install textfsmgen

```

Requirements:

- Python 3.9+
- pip or pipx

Install development dependencies:

```
git clone https://github.com/Geeks-Trident-LLC/textfsmgen.git (github.com in Bing)
cd textfsmgen
pip install -e .
```