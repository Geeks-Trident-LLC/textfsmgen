# Usage Overview

textfsmgen provides tools for:

- Validating TextFSM templates
- Parsing CLI output into structured data
- Generating template explanations
- Running via CLI or Python API

Two primary workflows:

1. **CLI usage** — for quick validation and parsing  
2. **Python API usage** — for integration into automation tools
